// 03)​ Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

const toPow = (base,exponent) => {
    return console.log((Math.pow(base, exponent)))
}

toPow(2,3)