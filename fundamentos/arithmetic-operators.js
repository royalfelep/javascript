const [a, b, c, d] = [2, 4, 3, 15]

const soma = a + b + c + d
const subtracao = b - a
const multiplicacao = c * d
const divisao = d / c
const modulo = b % c

console.log(soma, subtracao, multiplicacao, divisao, modulo)